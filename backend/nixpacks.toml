[phases.setup]
nixPkgs = ["nodejs_20", "python3", "make", "gcc"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npx prisma generate"]

[start]
cmd = "node server.js"
